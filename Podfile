# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'JKDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for JKDemo
  pod 'demo', :path => './' #podfile 所在相对路径

  target 'JKDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'JKDemoUITests' do
    # Pods for testing
  end

end
